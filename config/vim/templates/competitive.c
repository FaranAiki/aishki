#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef long long ll;
typedef unsigned long long ull;

#define loop(u) for (int i = 0; i < u; i++)

int main() {
	int n,m;
	scanf("%d %d", &n, &m);

	return 0;
}
